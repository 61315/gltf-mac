osx_image: xcode12.2
language: objective-c

script:
- set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace Example/GLTFMac.xcworkspace -scheme GLTFMac-Example -sdk iphonesimulator14.2 ONLY_ACTIVE_ARCH=YES | xcpretty
- pod lib lint
